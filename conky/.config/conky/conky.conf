conky.config = {
    background = true,
    update_interval = 1,
    double_buffer = true,
    own_window = true,
    own_window_type = 'desktop',         -- try 'override' or 'dock' if 'desktop' causes issues
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_argb_value = 150,         -- transparency
    alignment = 'top_right',
    gap_x = 30,
    gap_y = 30,
    minimum_width = 200,
    minimum_height = 50,
    use_xft = true,
    font = 'DejaVu Sans Mono:size=16',
    draw_shades = false,
    draw_outline = false,
    draw_borders = false,
    draw_graph_borders = false,
    default_color = 'white',
};

conky.text = [[
${time %H:%M:%S}
${time %A, %d %B %Y}
]];
